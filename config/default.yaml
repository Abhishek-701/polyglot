# Base config, overlaid by config/profiles/<naive|tuned>.yaml.
# Model routing, thresholds, and per-language tuning are added in later
# milestones (languages.yaml, thresholds.yaml, sources.yaml) as those
# components are built.

languages:
  - en
  - es
  - hi
  - zh # swapped from Tagalog in M4 — no TTS engine we evaluated supports
      # Tagalog at all (docs/tts_language_matrix.md); Mandarin is covered
      # by all three candidate engines. See PROGRESS.md M4.
